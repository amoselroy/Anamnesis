# SESSION CHUNK 2026-06-17 — Executing PLATFORM_HANDLERS Refactoring — Consolidati

*ID: passage-d538d7d7-15d2-4d96-b4b4-7d76cb32433f*
*Created: 2026-06-17*

---

SESSION CHUNK 2026-06-17 — Executing PLATFORM_HANDLERS Refactoring — Consolidating Site-Specific Logic into Registry Pattern

STRUCTURED
Files: C:\Users\Amos\projects\fb-poster\test_recdesk.py, C:\Users\Amos\projects\fb-poster\event_scraper.py, C:\Users\Amos\projects\fb-poster\test_handlers.py
Errors: Exit code 1
  File "<string>", line 11
    text = page.evaluate('() => document; Exit code 1
Raw text length: 15867
Raw HTML length: 200821
Cleaned text (6000 ; <tool_use_error>String to replace not found in file.
String: print(f"\n--- First
Tools used: PowerShell, Write, Edit, Read, Grep, ToolSearch, WebFetch, Glob, Bash
URLs: https://hoboken.recdesk.com/Community/Calendar", https://www.hobokennj.gov/resources/farmers-market

SUMMARY
Daimon read the full 1548-line `event_scraper.py` file to understand all the locations that needed refactoring. The refactor was then executed in 8 distinct edits: (1) adding `from dataclasses import dataclass` import; (2) creating `SiteHandler` dataclass with 5 fields; (3) creating `PLATFORM_HANDLERS` registry dict with 11 domain-fragment entries covering all known sites (libcal, tribe, squarespace, eventbrite, arthouseproductions, facebook, recdesk, and others) plus the newly-added RecDesk handler; (4) creating `_get_handler(url)` lookup function; (5) refactoring `extract_platform()` to use handler dispatch instead of 5 separate `if domain in` checks; (6) refactoring `passes_location()` to use `handler.always_accept` instead of checking `_ALWAYS_ACCEPT_DOMAINS` set; (7) refactoring `normalize_venue()` to use `handler.venue_suffix` instead of hardcoded libcal/libnet checks; (8) refactoring `scrape_source()` to use `handler.listing_mode` instead of checking `_PW_LISTING_DOMAINS` set and explicit URL patterns. A routing test confirmed 13/13 correct dispatch paths. A dry-run import and full module scan confirmed no syntax errors. The facility `<select>` stripping was also added to `_clean_text_for_llm()` to benefit all listing LLM calls. Lastly, a JSON parsing bug in `extract_listing_events()` was fixed: the LLM sometimes returns valid JSON array `[]` followed by explanation text, which breaks `json.loads()`. The fix strips leading/trailing non-JSON text before parsing. All changes were committed. The refactoring had zero behavior change — it was purely architectural consolidation that makes adding future sources (like RecDesk) trivial: just add a registry entry and implement the extractor function, no scattering changes across multiple functions.
