# SESSION CHUNK 2026-06-17 — RecDesk Empirical Validation and PLATFORM_HANDLERS Ar

*ID: passage-168f4adf-c22e-40a1-a0b9-87822e5f0675*
*Created: 2026-06-17*

---

SESSION CHUNK 2026-06-17 — RecDesk Empirical Validation and PLATFORM_HANDLERS Architectural Refactoring Approval

STRUCTURED
Files: C:\Users\Amos\projects\fb-poster\test_recdesk.py, C:\Users\Amos\projects\fb-poster\event_scraper.py, C:\Users\Amos\projects\fb-poster\test_handlers.py
Errors: Exit code 1
  File "<string>", line 11
    text = page.evaluate('() => document; Exit code 1
Raw text length: 15867
Raw HTML length: 200821
Cleaned text (6000 ; <tool_use_error>String to replace not found in file.
String: print(f"\n--- First
Tools used: PowerShell, Write, Edit, Read, Grep, ToolSearch, WebFetch, Glob, Bash
URLs: https://hoboken.recdesk.com/Community/Calendar", https://www.hobokennj.gov/resources/farmers-market

SUMMARY
The session resumed after a context boundary with the previous work on event scraper source validation and a proposed architectural refactoring. Daimon presented a proposal to consolidate scattered site-specific handling (which existed in 4 separate locations: `extract_platform()`, `scrape_source()` special listing paths, `passes_location()`, and `normalize_venue()`) into a single `PLATFORM_HANDLERS` registry pattern. Before committing to the refactor, Daimon proposed consulting an advisor (implicit pattern-matching to earlier sessions about seeking counsel before major changes). Amos had asked whether there was a clean way to separate universal algorithm from site-specific data. The architectural proposal used a `SiteHandler` dataclass with fields for `listing_mode`, `listing_extractor`, `detail_extractor`, `always_accept`, and `venue_suffix`, keyed by domain in a registry dict. Before building the RecDesk handler specifically (which was the catalyst for the refactor), Daimon tested the RecDesk page empirically via Playwright to verify it contained extractable events. The test confirmed: page renders with 15,867 characters of text, contains 76 time-tagged events, facility dropdown consumes ~2000 chars (a potential noise concern for the LLM window), but events are clearly visible in the rendered content. The question of stripping the facility `<select>` dropdown was discussed: Amos raised whether the cost was worth it, Daimon clarified the cost is identical (6000-char window limit is token-based, not byte-counted), so the only concern was filtering accuracy. Amos suggested checking whether the approach could use a facility whitelist instead of full calendar scraping, but Daimon noted all facilities are in Hoboken and since `always_accept=True` already handles location, facility filtering would only reduce noise, not improve accuracy. The `<select>` strip was approved as sufficient. Amos then approved the full refactoring with "yes, let's bite the bullet."
